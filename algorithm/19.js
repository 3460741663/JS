function printMatrix(matrix)
{
  // write code here
  // 四种情况 恰好成环、row 或 col剩余、落单
  let row = matrix.length;
  let col = matrix[0].length;
  if(!row % 2 && !col % 2){

  }

}
function trace(matrix, loops){
  for(let i = 0; i < loops; i++) {
    
  }
}
module.exports = {
    printMatrix : printMatrix
};